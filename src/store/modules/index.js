import toast from "./toast";
import posts from "./posts";
import users from "./users";
import nav from "./nav";
import tags from "./tags";

export default {
  // toast: toast 자바스크립트는 앞뒤가 같으면 생략가능
  toast,
  posts,
  users,
  nav,
  tags,
};
